import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(({ spacing }) => ({
    Hr: { width: '100%', height: '2px', margin: spacing(2, 0) },
}));

export default useStyles;
