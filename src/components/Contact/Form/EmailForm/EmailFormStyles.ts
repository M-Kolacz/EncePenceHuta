import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles((theme) => ({
    FormContainer: { width: '75%', margin: '0 auto' },

    TextField: {
        width: '100%',
    },
}));

export default useStyles;
